<% if group.is_active %>
  <%= link_to "有効", active_switch_admin_group_path(group.id), method: :patch, class: "btn btn-sm m-1 btn-success", remote: true %>
<% else %>
  <%= link_to "無効", active_switch_admin_group_path(group.id), method: :patch, class: "btn btn-sm m-1 btn-danger", remote: true %>
<% end %>