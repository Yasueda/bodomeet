<div class="d-flex align-items-center">
  <div class="heading col-lg-8 col-md-6">
    <span class="h2">お気に入りイベント（<%= @events.present? ? @events.total_count : 0 %>）</span>
  </div>
</div>

<%= render "layouts/events/list", events: @events %>